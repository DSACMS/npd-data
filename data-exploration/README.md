The data-exploration folder currently contains:

- a Jupyter Notebook file with code to download the data and read samples of each resource into Pandas DataFrames for exploration
- a schema in CSV format (generated by the Jupyter Notebook)
